import { chiton } from './chiton';

describe('chiton', () => {
  test('should return the expected result', () => {
    const input = [];
    const result = chiton(input);

    expect(result).toEqual(null);
  });
});
