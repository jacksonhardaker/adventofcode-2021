import { processInput } from '@adventofcode-2021/util-io';
import { <%= func %> } from './<%= func %>';

(async () => {
  const input = await processInput('assets/input.txt', { root: __dirname });
  const result1 = <%= func %>(input);

  console.log(`part 1: ${result1}`);
})();
